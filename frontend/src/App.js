import React,{useContext}from 'react';
import { Context } from './Store';
import './App.css';

function App() {
  // const {name} = useContext(Context);
  return (
    <div className="App">
    </div>
  );
}

export default App;
